{% set name = "contact_map" %}
{% set version = "0.3.1" %}
{% set sha256 = "ec4a7f88938b3f8eb9232ddaff233ce895b57a3ab4512de26f175f6a447f807e" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - python
    - setuptools
    - pip

  run:
    - python
    - future
    - numpy
    - mdtraj
    - pandas

test:
  imports:
    - contact_map

about:
  home: http://github.com/dwhswenson/contact_map
  license: LGPL-2.1+
  license_family: LGPL
  license_file: LICENSE
  summary: 'Contact maps based on MDTraj'
  description: |
      Contact maps based on MDTraj; useful for studying for intramolecular
      and intermolecular contacts from simulations of biomolecular systems.
      For a more detailed description, see package's documentation.
  dev_url: https://github.com/dwhswenson/contact_map

extra:
  recipe-maintainers:
    - dwhswenson
